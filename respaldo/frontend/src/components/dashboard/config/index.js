import { ListingPaymentTypes } from "./paymentType/listing";

const Config = (props) => {
  return (
    <div className="container-fluid">
      <ListingPaymentTypes />
    </div>
  );
};

export default Config;
